# Trae 账号管理工具 V1.0.6 更新说明

## 🚀 主要更新

### 1. TraeWork 每日签到

- TraeWork 面板新增单账号「签到」与顶部「全部签到」；自动签到（GUI 启动 / 开机 `--silent` 无头）一并纳入，每天无需手动操作
- 签到凭据直接复用账号快照里的登录态（与积分查询同一套解析：当前账号读实时现场、其余账号读快照），实测接口一次通过
- 无可达凭据的账号会**跳过**并提示自救动作（切换到该账号 → 点「保存当前登录态」），不会记为失败，也不会留下任何冷却或错误状态
- 凭据真正过期时进入 12 小时 `credential_stale` 冷却，重新「保存当前登录态」即自动解除；签到设备号与 TraeCode 侧彻底分域，互不干扰

### 2. TraeWork 面板视觉统一

- 面板与 TraeCode 账号页共用同一套设计组件：页标题（品牌竖条）、列表行（分隔线 + 悬停高亮，去掉旧的"盒中行"）、徽标（状态面 / 信息面两规格）、按钮（主 CTA / 次级 / 破坏性变体）与进行中遮罩
- 亮色 / 暗色两套主题下与账号页观感一致，全部颜色走语义变量

---

# Trae 账号管理工具 V1.0.4 更新说明

## 🚀 主要更新

### 1. 真正的单文件绿色版