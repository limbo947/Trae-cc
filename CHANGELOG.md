# 📋 更新日志

所有项目的显著变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
并且本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.0.6] - 2026-09-20

### ✨ 新增功能

- TraeWork 每日签到接入：面板支持单账号「签到」与「全部签到」，自动签到（GUI 启动 / `--silent` 无头）一并纳入。凭据按「当前槽读实时现场 → 主槽快照 → `.bak`」批次级解析（与积分查询共用同一实现）。无可达凭据的账号记 `skipped` 并提示自救动作（切换 + 重新保存登录态），不算失败、不落任何持久化状态
- TraeWork 凭据失效自救：过期凭据落 `credential_stale` 冷却（12 小时），在面板「保存当前登录态」后自动清除；TraeWork 账号的签到设备号按 `traework:{uid}` 前缀种子派生并启动回填，与 TraeCode 侧彻底分域

### 🎨 界面

- TraeWork 面板视觉与设计层统一：页标题、列表行（去盒化分隔线）、徽标（状态面/信息面两规格，取消 pill）、按钮（主 CTA / 次级 / 破坏性变体）与进行中遮罩全部复用 `trae-*.css` 设计层组件形态与语义变量，亮暗两套主题与 TraeCode 账号页观感一致

### 🔧 优化改进

- 窗口大小与位置持久化：关闭时记住窗口几何（含最大化状态），下次启动原样恢复。最大化时保留「最大化之前」的尺寸；上次位置所在显示器已不在（如拔掉外接屏）时只恢复尺寸、位置交由系统决定，不会把窗口丢到看不见的地方

---

## [未发布]

### ✨ 新增功能

- 新增 TraeWork（TRAE SOLO CN）账号管理：侧边栏独立页面，支持「保存当前登录态」与一键切换。TraeWork 的登录真源是 `storage.json` + `state.vscdb` 双源（后者为带加密 secret storage 的 SQLite），无法像 TraeCode 那样改写登录态，因此采用**登录态快照 / 恢复**：关闭客户端 → 备份现场 → 覆盖目标账号快照 → 重启。含 uid 证据链推导（置信度不足时拒绝识别而非猜测）、`.bak` 单代回退、快照体积展示与删除、可执行文件五级自动定位

### 🗑️ 移除

- 移除"快速注册"与"扫码领号"功能：添加账号弹窗只保留浏览器登录（含"从 Trae 读取"与导入/导出），同步删除 `QuickRegisterModal` / `ErrorModal` 组件、前端 API 封装、Rust 侧 `quick_register_backend` / `custom_tempmail` / `quick_register_simple` 模块及相关命令与设置项

### 🎨 界面

- 侧边栏「账号管理」更名为「TraeCode」：与 TraeWork 并列后语义更准确（统计页内的相关提示文案同步更新）
- 账号列表页移除工具栏邮箱搜索框，并清理 `App.css` / `trae-components.css` 中随之失效的样式
- 用量条改为「绿色 = 剩余额度、灰色 = 已消耗」：卡片与列表两处口径统一（抽 `utils/usage.ts` 的 `leftPercentOf`），取消原先按使用率切换红/黄/绿的三档配色，通用 / Work / 赠送子条也不再各自配色

### 🐛 Bug 修复

- 修复卡片/列表视图切换未持久化：视图偏好写入 `settings.json` 的 `view_mode`，与主题共用同一真源（不新增浏览器存储），关闭应用后不再回落为卡片视图

### 🔧 优化改进

- 账号详情弹窗数据展示调整：移除没有真实数据来源的"其他配额"（Slow Request / Advanced Model / Autocomplete 为后端默认值）与不适配 CN 版的美元/请求次数分支，补上用户 ID 与今日签到状态，额度区增加使用进度条
- 清理 `App.css` 中已无引用的扫码领号弹窗样式（约 760 行：`.quick-register-*` / `.qrcode-*` / `.step-*` / `.stats-badge-*` 等）
- 适配 Rust 1.81+ 的 `std::panic::PanicHookInfo`，消除 `cargo check` 的 deprecation 警告（现在零警告）

---

## [1.0.4] - 2025-03-24

### ✨ 新增功能

- 新增浏览器自动登录功能 - 支持输入邮箱密码后后台自动打开浏览器完成登录

### 🐛 Bug 修复

- 修复"添加账号"弹窗空白问题 - 恢复浏览器登录所需的状态变量
- 修复统计数据页面显示调试信息的问题 - 移除开发调试代码

### 🔧 优化改进

- 优化"添加账号"弹窗布局 - 整合浏览器登录和快速注册功能
- 删除未使用的浏览器注册相关代码，简化代码结构

---

## [1.0.3] - 2025-03-22

### ✨ 新增功能

- 导入/导出功能移到"添加账号"弹窗中，操作更便捷
- 增加额度筛选功能 - 支持下拉框筛选有剩余额度和无剩余额度的账号
- 当前使用账号自动置顶显示

### 🐛 Bug 修复

- 修复切换账号时 Trae IDE 闪退问题 - 避免重复启动 Trae 导致的竞争条件
- 修复机器码被 Trae 自动覆盖的问题 - 移除对 `storage.json` 中 telemetry ID 的修改，只更新 `machineid` 文件
- 修复统计数据不显示的问题 - 添加 serde 默认值属性防止解析失败

### 🔧 优化改进

- 优化切换账号流程，确保 Trae 只被启动一次
- 改进错误处理逻辑，避免闭包问题导致的错误状态设置不正确
- 优化"添加账号"弹窗布局，快速注册作为独立按钮显示

---

## [1.0.0] - 2025-03-15

### ✨ 新增功能

- 🚀 快速注册功能 - 自动生成临时邮箱并自动完成注册流程
- 🎨 全新 UI 界面 - 重构整个界面设计，更加美观易用
- 📊 账号使用量实时监控
- 🔄 一键切换账号功能
- 📥 账号导入/导出功能
- 🌙 深色/浅色主题切换

### 🔧 优化改进

- 优化页面加载速度
- 改进账号管理逻辑
- 优化错误提示信息
- 改进表单验证机制

### 🐛 Bug 修复

- 修复快速注册时验证码获取问题
- 修复 Token 拦截失败的问题
- 修复邮箱验证码解析问题
- 修复若干界面显示问题

---

## [2.0.0] - 2025-02-XX

### ✨ 新增功能

- 基于 Tauri 2.0 重构项目架构
- 新增 Rust 后端处理
- 新增多账号管理功能
- 新增机器码管理功能

### 🔧 优化改进

- 从 Electron 迁移到 Tauri，大幅减小安装包体积
- 提升应用启动速度
- 优化内存占用

---

## [1.0.0] - 2025-01-XX

### ✨ 初始版本

- 基础账号管理功能
- Trae IDE 集成
- 简单的 UI 界面

---

## 版本说明

- **主版本号**：当进行不兼容的 API 修改时递增
- **次版本号**：当添加向下兼容的功能时递增
- **修订号**：当进行向下兼容的问题修复时递增

---

## 如何贡献

欢迎提交 Issue 或 Pull Request 来完善更新日志！

---

**维护者**: HJH
